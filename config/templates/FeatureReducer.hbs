/**
 * {{ feature }} REDUCER 
 */

import { Reducer } from "redux";
import { {{feature}}State } from "@src/store";
import { ActionTypeKeys } from './{{feature}}Constants';
import * as Interfaces from './{{feature}}Constants';

export type {{feature}}Actions = Interfaces.
    | Interfaces.;

const initialState = {
};

const {{feature}}Reducer: Reducer<{{feature}}State> = (state: {{feature}}State = initialState, action: {{feature}}Actions) => {
  switch (action.type) {
    case :
      return { ...state };
    default:
      return state;
  }
};

export default {{feature}}Reducer;
