import * as React from 'react';

import MonthTracker from ''

class Tracker extends React.Component {
  render() {
   return (
    <div className="card">
      <h4>Tracker</h4>
      <MonthTracker />
    </div>
   );
 }
}

export default Tracker;