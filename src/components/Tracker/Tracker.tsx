import * as React from 'react';

class Tracker extends React.Component {
  render() {
   return (
    <div className="card">
      <h4 className="placehold__title">Tracker</h4>
    </div>
   );
 }
}

export default Tracker;